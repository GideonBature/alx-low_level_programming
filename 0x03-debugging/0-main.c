#include "main.h"
/**
 * main - a test for function that checks for  positive/negative
 * Return: Always 0 (successful)
*/

int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);
	return (0);
}
